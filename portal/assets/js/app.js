// La entrada de Webpack Encore es assets/css/app.scss (ver webpack.config.js).
// Este fichero no se incluye en el build: evita babel-loader en el servidor.
// Si necesitas una entrada JS, vuelve a añadir .addEntry('app', './assets/js/app.js') y fija Babel 7.8.x + npm ci.
